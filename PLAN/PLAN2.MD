现在我要继续完善这个项目，你是专业的phython专家和房产研究员，结合我的想法和项目当下的实际，给出中肯以及可行的评估报告及解决方案，一起完善这个项目。请全部用中文回复。所有的改动均建立在现有项目的基础上，按照最小改动原则，如果于关键改动一定要提出来，不能擅自修改，因为这里面的工作流真的很重要，请务必保留，避免重复工作。 如果评估报告及解决方案多次更新，请采用内容叠加扩展的方式，如果不是前后矛盾不能删除前文。
我今天运行了一次项目，观察报告和数据后，认为这个项目有以下事项可能会成为问题：
1、项目启动后，进入项目菜单，像游戏一样，有新建、保存、加载、删除、参数设置等选项。
关于参数设置选项，应该有如下功能：
A输入随机种子((使得项目的可复现性 ， 变量可控制。可复现性可以通过引入 Random Seed (随机种子) 机制。输入种子 1234，无论跑多少次，结果都应该一模一样。其实这个问题在3天前已经解决过，如@implementation_plan.md.resolved 所示，但是在后续的完善中被覆盖了，造成重复工作。))
B设置agent的情况：设置收入分界线-将所有agent根据收入水平和资产水平划分为5个档次，超高收入人群，高收入人群，中收入人群，中低收入人群以及低收入人群，要求用户输入年收入金额，作为划分agent档次的分界线；然后，设置5个档次分别对应多少数量的agent，以及每个档次分别拥有房产的人数。在项目每次完成测试后，生成关于agent的wealth_distribution.png (财富分布图)，包括时点情况和时序变化，直观展示贫富差距变化（如果环境支持）。
C设置房产情况，设定房产数量（不能少于每个档次分别拥有房产的人数之和，如果设定房产数量大于每个档次分别拥有房产的人数之和，意味着某个agent可以分配两套以上的房产）登记入房产数据库，与agent数据库通过agent id形成关联。
D设置宏观环境。大众收入的预期等等（稳定，积极，消极？我也不知道该具体该怎么落地，不过我觉得不应该让每个agent自行决定不可控）
2、关于项目启动后agent的完整生成流程。首先，请看  ，有agent_id，但是没有name，另外，LLM驱动每个agent的故事背景，发现存在故事背景提及的名字重复（陈默），agent_id=1的cash=144426，竟然生成的故事存在“高强度的职业生涯让他积累了可观的财富”这样的描述，出现不匹配的情况。第二，存在大量字段“agent_id,name,age,marital_status,monthly_income,cash,occupation,career_outlook,family_plan,education_need,housing_need,selling_motivation,background_story,profile_summary,created_at”，且部分字段为空白，但是是否会对agent模拟真实人物进行房产买卖交易有帮助，是否存在多余的，可能需要整改。最后，如果后期用LLM驱动做100个甚至1万个agent，恐怕会耗费大量token，我在想预设是否是否存在人生百态始终有迹可循，应该是可以根据年龄、婚姻状态、子女情况、职业、持有资产和月收入等要素进行不同的组合，预设20到50种agent人设基本模板（还要限定全部agent有三个状态可以切换，买家/卖家/观察者，建议默认是观察者），所以，我想细化agent的生成到刷新agent状态的流程：
项目每次启动后，按用户设置或默认参数生成5个档次的人群，仅需要对生成的agent数据库，匹配agent人设基本模板（比如高收入低资产可能是刚入职的单身高水平人才，也可能是初创成功人士，低收入高资产可能是未婚富二代，已婚退休人士，低收入低资产可能是打工人，家道中落的村二代等等），匹配完成后，形成含有关键字段且有人设背景的agent，aagnt的身份（建议默认观察者）由并将这些信息全部写入数据库，方便后面由LLM根据agent数据库的情况驱动后续行为。这个阶段agnet带出的信息是中立的，还没对agent是买家/卖家/观察者的身份进行判定。那么，下一步，联动agent数据库以及房产数据库的数据，将agent信息导入到LLM，由LLM挖掘每个agent关键买房节点：小孩上学以及换更好的学校，工作变动，结婚等等需求是应该由LLM进行驱动，形成买卖房产的明确需求，从而判定agent身份（低收入低资产基本就可以略过不用判断agent身份），进而再根据agent_id（ 和 对于agent的id表述不一致，下文讨论）找到对应房产，LLM选择出拟房产的房产，改成for sale状态，再通过里面的工作流 进行后续的谈判等流程。（这个是我的想法，这样会不会减少LLM的负荷，比@agents.csv 这样的一次过生成的情况更快更可靠，看看你有什么建议）

3、关于谈判撮合。请检查目前的买卖双方定价机制，因为目前多轮谈判是通过LLM落地的，但是并没有说清楚供过于求或者供不应求双方的买卖策略，在供不应求的情况下，“价高者得”，是否能在LLM驱动下落地；在供过于求的市场环境下，如果交易失败，买房成为观察者，卖方真的不会调低价格吗？这个需要请你结合目前项目跟我进行探讨。

4、关于房产，根据 ，暂时保持property_id,zone,quality,base_value,building_area,bedrooms,unit_price,property_type,is_school_district,school_tier,owner_id,status,listed_price,last_transaction_month的字段可能可以简化，比如bedrooms再项目实践中大概率不需要，另外要注意房产数据库的owner_id和agent数据库的agent_id是关联的，通过跟踪agent的id可以找到其名下持有哪些房产房产，通过房产数据库的owner_id可以查到被该房产被谁持有。但是数据库是否因为owner_id和agent_id字段命名不同不能产生关联，从而影响刷新agent状态时（假如变成卖家），对应status的变化（变成for sale）。